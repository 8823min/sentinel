# Sentinel Bot 起動方法

## 前提条件

1. Node.jsがインストールされていること（バージョン18以上）
2. `.env`ファイルがプロジェクトルートに存在し、`DISCORD_TOKEN`が設定されていること
3. 依存関係がインストールされていること（`npm install`を実行済み）

## 起動方法

### 方法1: npm start（通常起動）

```bash
npm start
```

- Botが起動し、Discordに接続します
- コンソールにログが表示されます
- 停止するには `Ctrl + C` を押します

### 方法2: npm run dev（開発モード）

```bash
npm run dev
```

- ファイル変更を自動検知して再起動します
- 開発中に便利です
- 停止するには `Ctrl + C` を押します

### 方法3: nodeコマンドで直接起動

```bash
node src/index.js
```

## 起動確認

Botが正常に起動すると、以下のようなメッセージが表示されます：

```
✅ sentinel#xxxxx としてログインしました
📊 接続中のサーバー数: X
✅ グローバルコマンドを登録しました（反映まで最大1時間かかる場合があります）
```

または、ギルドコマンドとして登録した場合：

```
✅ sentinel#xxxxx としてログインしました
📊 接続中のサーバー数: X
✅ ギルドコマンドを登録しました（Guild ID: xxxxx）
```

## トラブルシューティング

### Botが起動しない場合

1. `.env`ファイルに`DISCORD_TOKEN`が正しく設定されているか確認
2. Node.jsのバージョンが18以上か確認：`node --version`
3. 依存関係がインストールされているか確認：`npm install`
4. エラーメッセージを確認

### コマンドが表示されない場合

- グローバルコマンドの場合、反映まで最大1時間かかります
- 即座にテストしたい場合は、`.env`に`DISCORD_GUILD_ID`を設定してください

### Botがサーバーに参加していない場合

- Discord Developer PortalでBotをサーバーに招待してください
- Botに必要な権限（Manage Roles, Manage Channels等）を付与してください

